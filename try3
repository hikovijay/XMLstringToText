for (int colNum = 0; colNum < 5; colNum++) {
            sheet.autoSizeColumn(colNum);
        }

        // Save the Excel file
        try (FileOutputStream outputStream = new FileOutputStream("AutoAdjustColumnsExample.xlsx")) {
            workbook.write(outputStream);
        } catch (IOException e) {
            e.printStackTrace();
        }
