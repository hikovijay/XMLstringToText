HttpURLConnection connection = (HttpURLConnection) new URL("https://example.com/your_page").openConnection();
int statusCode = connection.getResponseCode();
if (statusCode == 404) {
    // Handle the "Page Not Found" error
}


try {
    driver.get("https://example.com/your_page");
} catch (org.openqa.selenium.TimeoutException e) {
    // Handle the timeout error (e.g., log the error, take a screenshot, or perform another action)
}
