for (int rowNum = 1; rowNum <= sheet.getLastRowNum(); rowNum++) {
                Row row = sheet.getRow(rowNum);
                if (row != null) {
                    // Get the cell in column B
                    Cell cellB = row.getCell(1); // Column B is index 1

                    // Check the data type of cell B (assuming it's a string)
                    if (cellB.getCellType() == CellType.STRING) {
                        String valueB = cellB.getStringCellValue();

                        // Compare valueB with the string array
                        for (int i = 0; i < stringArray.length; i++) {
                            if (valueB.equals(stringArray[i]) && i < stringArray.length - 1) {
                                // Match found, get the next string from the array and update column D
                                String valueD = stringArray[i + 1];
                                Cell cellD = row.createCell(3, CellType.STRING); // Column D is index 3
                                cellD.setCellValue(valueD);
                                break; // Exit the loop after a match is found
                            }
                        }
                    }
                }
            }
