int rowNumber = 2; // Row number (0-based index) where you want to write the value
            int columnNumber = 1; // Column number (0-based index) where you want to write the value

            Row row = sheet.getRow(rowNumber);
            if (row == null) {
                row = sheet.createRow(rowNumber);
            }

            String valueToWrite = "Your String Value"; // Replace with the value you want to write
            Cell cell = row.createCell(columnNumber);
            cell.setCellValue(valueToWrite);
