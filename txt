Row row = sheet.getRow(rowNum);
            if (row == null) {
                row = sheet.createRow(rowNum);
            }

            Cell cell = row.createCell(colNum);
            cell.setCellValue("Your String Value"); // Replace with your string value
